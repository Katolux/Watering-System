# Encyclopedia botanical images

Drop a transparent `.webp`, `.jpg`, `.jpeg`, or `.png` file here using the
plant record's `icon_key` as its filename.

Example: a record with `icon_key: "tomato"` automatically uses `tomato.png`.
When no botanical image exists, the Encyclopedia uses the shared GardenHub
plant placeholder.
