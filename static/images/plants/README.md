# Encyclopedia plant images

Drop a `.webp`, `.jpg`, `.jpeg`, or `.png` file here using the plant record's
`photo_key` as its filename.

Example: a record with `photo_key: "tomato_01"` automatically uses
`tomato_01.webp` when that file is present. No route or template change is
required.
